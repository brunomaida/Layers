# mock.html pendente

Abra `Painel CCRC.html` (ou o export desejado) no Chrome, F12 › Console, cole `tools/layers-snapshot.js` e Enter. Salve o download como `layers/mock.html` e ajuste `viewport` em `layers.json` com o valor sugerido no console.
